# 🚀 Деплой бота на Render.com

Этот гайд поможет вам развернуть Telegram бота на Render.com, чтобы он работал 24/7.

## 📋 Требования

- GitHub аккаунт (бесплатный)
- Render.com аккаунт (бесплатный)
- Telegram Bot Token

---

## 🔧 Шаг 1: Подготовка GitHub репозитория

### 1.1 Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите **"New repository"**
3. Назовите его `telegram-stock-bot`
4. Выберите **Public** (если хотите приватный, нужна платная подписка Render)
5. Нажмите **"Create repository"**

### 1.2 Загрузите ваш код

```bash
cd "/Users/sergey/Work/AI PROJECTS/CHATBOT"
git init
git add .
git commit -m "Initial commit: telegram bot"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/telegram-stock-bot.git
git push -u origin main
```

**Замените `YOUR_USERNAME` на ваш GitHub username**

> ⚠️ Убедитесь, что `.env` файл в `.gitignore` - токены не должны коммититься!

---

## 🔐 Шаг 2: Регистрация на Render.com

1. Перейдите на [render.com](https://render.com)
2. Нажмите **"Sign up"** → выберите **GitHub**
3. Авторизуйтесь на GitHub
4. Разрешите Render доступ к вашим репозиториям

---

## ⚙️ Шаг 3: Создание Web Service на Render

1. На dashboard Render нажмите **"New +"** → **"Web Service"**

2. Выберите ваш репозиторий `telegram-stock-bot`

3. Заполните параметры:

   | Параметр | Значение |
   |----------|----------|
   | Name | `telegram-stock-bot` |
   | Environment | `Python 3` |
   | Region | Germany (Frankfurst) или ближайший |
   | Branch | `main` |
   | Build Command | `pip install -r requirements.txt` |
   | Start Command | `python bot.py` |
   | Plan | **Free** |

4. Нажмите **"Create Web Service"**

---

## 🔑 Шаг 4: Добавление переменных окружения

На странице вашего сервиса на Render:

1. Перейдите в **"Environment"**
2. Добавьте переменные (нажимайте **"Add Environment Variable"**):

   ```
   TELEGRAM_BOT_TOKEN = ваш_токен_здесь
   OPENAI_API_KEY = ваш_openai_ключ (если есть)
   OPENAI_MODEL = gpt-4o-mini
   PORTFOLIO_DB_PATH = portfolio.db
   ```

3. Нажмите **"Save Changes"**

---

## ✅ Шаг 5: Проверка статуса

1. На странице сервиса в Render смотрите логи (вкладка **"Logs"**)
2. Ищите сообщение: `Starting bot at ...`
3. Если ошибок нет - бот работает! 🎉

---

## 🔄 Автоматическое обновление

**После каждого git push в main:**

```bash
git add .
git commit -m "Update: description of changes"
git push origin main
```

Render автоматически:
1. Заберёт новый код
2. Пересоберёт зависимости
3. Перезапустит бот

---

## 📊 Полезные ссылки

- **Логи бота**: В Render Web Service → "Logs"
- **Документация Render**: https://render.com/docs
- **Проси помощь**: Если что-то не работает, смотри логи в Render!

---

## ⚠️ Важные замечания

- ✅ **Бесплатный тариф** работает, но сервер может "уснуть" после 15 минут неактивности (покупка $7/мес убирает это)
- ✅ **БЛокировка портов** - Render автоматически назначит PORT, бот должен его игнорировать (используем polling, не webhooks)
- ✅ **Логи** доступны в реальном времени на Render dashboard
- ✅ **Перезагрузки** автоматические - бот переживает их без потерь

---

## 🆘 Если что-то не работает

### Ошибка: "TELEGRAM_BOT_TOKEN is not set"
- Проверьте, что вы добавили `TELEGRAM_BOT_TOKEN` в Environment на Render
- Убедитесь, что токен не пустой и без пробелов

### Ошибка подключения к интернету
- Это норма - Render имеет исходящий интернет, но может быть задержка
- Проверьте логи через 2-3 минуты

### Бот не отвечает
- Проверьте логи в Render (вкладка "Logs")
- Убедитесь, что OpenAI API ключ верный (если используется)

---

**Готово!** Ваш бот теперь работает 24/7 на облаке ☁️
